# Известные риски

> Перенесено из диагностики прода 2026-07-10 (старый `CLAUDE.md`) + разбор документации.
> **Ни один из них не исправлен** — только задокументирован. Каждый заслуживает задачи через `/tl`.

## 🔴 R-1. Дублирование Celery worker/beat — риск двойных списаний
`docker/start.sh` (строки 35–41) сам поднимает `celery worker` + `celery beat` в фоне **внутри** контейнера `educrm`. При этом в репозитории лежат `railway.worker.toml` и `railway.beat.toml` с теми же командами.

Через Railway MCP подтверждено: отдельных сервисов worker/beat в проекте `pleasing-trust` сейчас **нет**. Но конфиги вводят в заблуждение и опасны при следующем деплое.

**Последствие:** двойной запуск `daily_lesson_charge` = **двойное списание денег с кошельков учеников**. Плюс дублирование SMS-рассылок.
**Нужно:** выбрать целевую архитектуру (всё в одном контейнере ИЛИ отдельные сервисы) и убрать лишнее. Это ADR.

## 🔴 R-2. Логин перебирает схемы PostgreSQL по номеру телефона
Backend определяет тенант пользователя перебором схем.
**Последствие:** деградация линейно с ростом числа учебных центров — **прямо на пути масштабирования**. Плюс возможность user enumeration.
**Нужно:** индекс телефон→тенант в `public`-схеме + rate-limit на `/auth/token/` + одинаковый ответ для несуществующего номера.

## 🟠 R-3. `Group.status` ни на что не влияет
`recruiting` / `active` / `frozen` / `completed` нигде не влияет на бизнес-логику посещаемости и уроков.

⚠️ **Важно, проверено grep-ом по всему репозиторию 2026-07-10:** гарда «группа должна быть active» в `backend/apps/lessons/views.py` **не существует** — ни в бэкенде, ни во фронтенде, ни в тестах.
**Не добавляй такой гард «для надёжности» — его никогда не было. Это создаст новый баг, а не решит существующий.**
**Нужно:** явно решить, нужен ли гард вообще.

## 🟠 R-4. Единый Redis db/0 на три роли
Cache + Celery broker/backend + Channels layer в одной базе.
**Последствие:** конкуренция под нагрузкой, взаимное вытеснение.
**Нужно:** развести по разным db или инстансам.

## 🟠 R-5. Media на ephemeral-диске Railway
`/media/logos/logo_*.jpg` периодически отдаёт 404.
**Причина:** ephemeral filesystem Railway — файлы теряются при редеплое.
**Нужно:** persistent volume или S3/MinIO для media.

## 🟠 R-6. `/api/v1/attendance/` отдаёт ~39KB одним ответом
Замечено в логах прода.
**Нужно:** проверить пагинацию.

## 🟡 R-7. Один процесс Daphne
Сейчас один ASGI-процесс.
⚠️ Перед изменением подтвердить набор зависимостей (`gunicorn`/`uvicorn` в `requirements/`) — это критичный файл, менять вслепую нельзя.

## 🟡 R-8. 98 проверок роли вне permission-классов
`scripts/baseline.py` нашёл 98 мест вида `if user.role == ...` в `backend/apps/`.
**Последствие:** права размазаны по вьюхам, легко пропустить роль при изменении. Источник дыр в доступе.
**Нужно:** постепенно свести к permission-классам. Храповик не даёт числу расти.

## 🟡 R-9. PII несовершеннолетних в MinIO
Документы учеников: паспорта, свидетельства о рождении.
**Нужно:** presigned короткоживущие ссылки, валидация загрузки, отсутствие в логах.

## 🟡 R-10. Двойное начисление монет и списаний
Посещаемость и оценки триггерят монеты и списания. `is_charged` — единственный страж.
**Нужно:** убедиться, что он покрывает все пути, включая пересохранение.

## 🟠 R-11. Нет `.gitattributes` — 400 файлов CRLF-шума в рабочей копии
Обнаружено 2026-07-26. `core.autocrlf` не задан, `.gitattributes` отсутствует.
`git status` показывает **~400 изменённых файлов**, которые различаются **только окончаниями строк** (CRLF ↔ LF), содержимое идентично.

**Последствие:** `git add -A` закоммитит 400 файлов чистого шума. Ревью станет невозможным, `git blame` будет испорчен, конфликты при merge staging↔master вырастут на порядок. Это прямо мешает работе агентов — они не смогут отличить свои изменения от шума.

**Нужно:** добавить `.gitattributes` с `* text=auto eol=lf`, затем один раз нормализовать (`git add --renormalize .`) отдельным коммитом, помеченным как чисто механический.

**До этого:** коммитить только явно перечисленные файлы, **никогда `git add -A`**.

## 🟠 R-12. Незакоммиченная работа в рабочей копии
На 2026-07-26 в `src/` есть реальные незакоммиченные изменения (не CRLF-шум):
`components/edu/phone-input.tsx` · `status-badge.tsx` · `topbar.tsx` · `layouts/enterprise-layout.tsx` · `mobile-layout.tsx` · `sidebar-layout.tsx` · `students/create-student-sheet.tsx` · `lib/phone.ts` · `lib/theme.tsx`
Плюс неотслеживаемые: `src/components/edu/date-input.tsx`, `theme-toggle.tsx`, `backend/apps/superadmin/migrations/0004_alter_platform_name_default.py`, `public/brand/`

**Нужно:** решить судьбу этой работы (закоммитить или откатить) до запуска агентов. Иначе агент может случайно её затронуть или закоммитить вместе со своим изменением.
