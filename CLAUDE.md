# EduCRM — Claude Agent Instructions

## Проект
- Frontend: React + TypeScript + Vite (папка: корень проекта)
- Backend: Django + DRF (папка: backend/)
- БД: PostgreSQL (Railway)
- Деплой: Railway (автодеплой из ветки main)
- Репозиторий: https://github.com/rustambadev2024-ctrl/educrm

## Роли пользователей
- superadmin — суперадмин (public схема)
- director — директор филиала
- admin — администратор
- branch_admin — администратор филиала
- teacher — учитель
- support_teacher — помощник учителя
- student — студент

## Правила работы
1. ВСЕГДА читай этот файл первым
2. Перед изменениями — покажи что собираешься менять
3. После изменений — npm run build (проверка фронта)
4. После изменений — python manage.py check (проверка бэка)
5. Коммить только рабочий код

## Структура
- backend/apps/ — Django приложения
- src/routes/ — страницы фронтенда по ролям
- src/components/ — переиспользуемые компоненты
- src/lib/api.ts — все API вызовы

## 8 столпов UI — весь интерфейс строится по ним
Активные скилы: `frontend-design`, `design-system`, `shadcn`. Стек: TanStack Start + Tailwind v4 + shadcn/ui (new-york, radix, lucide). Всегда используй семантические токены (`bg-primary`, `text-muted-foreground`), `gap-*` вместо `space-y-*`, `size-*` вместо `w-* h-*`.

1. **Микроанимации и переходы.** Каждое действие имеет плавный отклик 150–300ms: карточки fade-in, панели выезжают, кнопка при нажатии «проваливается». Анимации shadcn Dialog/Sheet использовать как есть. Уважать `prefers-reduced-motion`.
2. **Типографика с иерархией.** С первого взгляда ясно: главное / второстепенное / подсказка. Разные размеры и веса (light/regular/semibold/bold), правильный line-height. Не делать всё одним размером.
3. **Состояния каждого элемента.** Минимум 5 состояний: обычное, hover, active, disabled, loading. Пользователь всегда понимает, можно ли кликнуть и что происходит.
4. **Пустые состояния.** Пустой список = компонент `Empty`: иконка + понятный текст («У вас пока нет студентов») + кнопка действия. Никогда просто белый экран.
5. **Обратная связь на каждое действие.** Клик → спиннер/«Сохраняем…» → `sonner` toast: зелёный «Сохранено» или красный с конкретной причиной. Ошибки прокидывают детали через `apiErrorMessage`, не голое «Xatolik».
6. **Детали для внимательного взгляда.** Аватар с инициалами — цвет детерминирован по имени, иконки выровнены с текстом (`data-icon`), чередование строк таблицы, тонкая цветная черта слева у активного пункта меню.
7. **Осмысленные цвета, не декоративные.** Красный = только опасность/удаление, зелёный = успех/активность, жёлтый = предупреждение, серый = неактивное. Цвет несёт информацию. Никаких `bg-blue-500` «потому что синий».
8. **Отзывчивость под контекст.** Не сжатый десктоп: на мобиле таблица → карточки, боковое меню → нижняя навигация, тач-таргеты ≥44px, модалки открываются снизу как `Drawer`/`Sheet`. student/ и parent/ порталы — mobile-first (`MobileLayout`).

### Подтверждения
Опасные и необратимые действия (удаление, отмена, возврат) подтверждаются через `ConfirmDialog` (`src/components/ui/confirm-dialog.tsx`) — НИКОГДА `window.confirm`.

## Последние коммиты
- 7baa75c — fix(security): close cross-branch IDOR, unguarded endpoints, brute force
- c16dbee — fix(multitenancy): login by password across tenants, close branch leak, green CI
- f34ba30 — Merge PR #9: списание в день урока, а не на день позже

## ТЕКУЩИЙ СТАТУС И ОТКРЫТЫЕ ЗАДАЧИ
(обновлено 2026-08-05, по итогам подготовки к выдаче системы другим клиентам)

### Открытые PR — влить по порядку
1. **PR #10** `fix/login-multi-tenant-phone` → master. CI зелёный.
2. **PR #11** `fix/security-hardening` → база #10. CI зелёный. После мержа #10
   база переключится на master сама.
3. PR #4 `fix/payment-reversal-and-ui-bugs` висит с 19 июля, не проверялся.

### Что сделано в этой сессии
- Биллинг списывал на день позже урока: расписание хранится пн=1..вс=7, а
  сравнивалось с `date.weekday()` (пн=0). Чинится вызовом `slot_weekday()` —
  конвертер уже существовал, биллинг его просто не звал. Тест
  `tests/test_billing_weekday.py` падает при возврате сдвига.
- Создан Railway Volume `educrm-volume` на `/app/media`. До этого тома не было
  вообще: логотипы, фото, документы и файлы ДЗ стирались при каждом деплое.
- Вход: один телефон мог быть в нескольких организациях, схема выбиралась
  произвольно и пароль проверялся уже в ней — человек получал 401 на верных
  данных. Теперь схема выбирается по подошедшему паролю.
- Закрыты дыры доступа (см. PR #11): история переводов без проверки роли,
  `teacher-checkin` доступный ученику, отчёт по группе / выгрузка зарплаты /
  перевод ученика без скоупа филиала, вход без лимита попыток, перебор номеров
  по разнице 400/401, публичный поиск сессии по коду без лимита.
- CI стал настоящим гейтом: раньше бэкенд-джоба падала всегда (63 ошибки
  flake8) и тесты не запускала вообще. Сейчас flake8 = 0, добавлен шаг pytest,
  73 теста проходят.

### Требует действий владельца
- **10 аккаунтов в kelajak_talim с тривиальными паролями** (2 учителя,
  5 учеников, 3 родителя) — нужен принудительный сброс. Список в истории
  сессии. Троттлинг теперь тормозит подбор, но не отменяет проблему.
- **Том Postgres — 500 МБ, занято 164 МБ.** Один клиент ≈ 25 МБ данных.
  Поднять до 10 ГБ в панели Railway до подключения новых клиентов.
- `educrm-volume` нарезан на 50 ГБ при реальном расходе 56 КБ — можно
  уменьшить, если Railway позволит.

### Известные проблемы, не тронутые
- **44 ошибки типов во фронтенде** — поэтому `tsc --noEmit` в CI не добавлен,
  гейт был бы вечно красным.
- `staff/serializers.py:validate_phone` блокирует телефон, занятый в другой
  организации: сотрудник не может работать в двух центрах. Противоречит
  правке входа. Убирать — решение владельца.
- Маршрутизация клиентов держится на заголовке `X-Tenant-Schema` из
  localStorage; у kelajak_talim домен `kelajak_talim.localhost`, нерабочий.
  Для доменов вида `школа.educrm.uz` нужен wildcard и DNS.
- `AUTH_PASSWORD_VALIDATORS = []` — политики паролей нет намеренно (в
  `staff/serializers.py` явно написано, что ограничения сняты).
- `ALLOWED_HOSTS=*` на проде; ключи MinIO по умолчанию `minioadmin` в
  `base.py` (S3 не используется, константы лежат мёртвым грузом).
- Дублирование celery worker/beat: `docker/start.sh` поднимает их внутри
  контейнера, при этом в репо лежат `railway.worker.toml` / `railway.beat.toml`
  с теми же командами. Отдельных сервисов в Railway сейчас нет — но при
  следующем деплое риск двойных списаний.
- Единый Redis на кэш + брокер Celery + Channels.
- Невлитая миграция `superadmin 0004_alter_institutionactionlog_action` —
  только `choices`, на схему БД не влияет.
- Комментарий в `tests/factories.py` про `schedule` (0=Mon..6=Sun) не
  соответствует реальной конвенции (1=Mon..7=Sun); `slot_weekday()` понимает
  оба варианта, поэтому фабрика работает.
