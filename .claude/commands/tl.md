---
description: Написать тех-лиду. Единственное, что нужно знать — опиши проблему своими словами
argument-hint: "например: учителя жалуются что не сохраняется посещаемость"
---

Человек написал: **$ARGUMENTS**

Ты — `tech-lead`. Прочитай `.claude/agents/tech-lead.md` и следуй ему буквально.

## Обязательный порядок

**1. Разведка.**
```bash
cat vault/00-Index/HOME.md
python scripts/state.py show
cat vault/60-State/open-issues.md
cat vault/30-Architecture/risks.md
```
Проверь: не расследовано ли это уже? Что уже исключено? Отправить агента искать то, что месяц назад исключили grep-ом — прямая потеря времени.

**2. Найди в коде, о чём речь.** `grep` по `src/` и `backend/apps/`. Определи затронутые модули, роли из 7, и попадает ли задача в красную зону (деньги · мультитенант · PII · права).

**3. Совещание.** Спавни профильных агентов через Agent-tool, независимых — параллельно в одном блоке. Каждому передай: запрос человека, ссылки на файлы vault и **конкретные файлы кода, которые нашёл при разведке**. Напомни, что он проходит протокол разведки (`vault/40-Standards/recon-protocol.md`) и вправе доустановить нужный MCP/скилл (`vault/40-Standards/tooling.md`).

**4. Спека.** `vault/60-State/features/F-XXX.md` по шаблону. Таблица по 7 ролям и «что НЕ входит» — обязательны. Нужен ADR — напиши.

**5. Задачи.** Через `scripts/state.py`, зависимости через `--after`.

**6. Покажи результат.** `python scripts/state.py show`

## Отчёт человеку
Что понял · что уже было известно из vault · кого спросил · что решили · какие задачи создал · кто первый в очереди · **что нужно от человека**.

Коротко. Спеку он откроет сам, если захочет.
